substitutions:
  _REGION: asia-southeast2
  _PROJECT: YOUR_DEV_PROJECT_ID  # override via --substitutions="_PROJECT=..." on submit
  _REGISTRY: asia-southeast2-docker.pkg.dev
  _REPO: tring-service

steps:
  - name: "gcr.io/cloud-builders/docker"
    id: build-ingestion
    args:
      - build
      - -t
      - ${_REGISTRY}/${_PROJECT}/${_REPO}/ingestion:$COMMIT_SHA
      - -t
      - ${_REGISTRY}/${_PROJECT}/${_REPO}/ingestion:latest
      - ingestion/

  - name: "gcr.io/cloud-builders/docker"
    id: build-dbt
    args:
      - build
      - -t
      - ${_REGISTRY}/${_PROJECT}/${_REPO}/dbt:$COMMIT_SHA
      - -t
      - ${_REGISTRY}/${_PROJECT}/${_REPO}/dbt:latest
      - transform/

  - name: "gcr.io/cloud-builders/docker"
    id: push-ingestion
    args: ["push", "--all-tags", "${_REGISTRY}/${_PROJECT}/${_REPO}/ingestion"]
    waitFor: ["build-ingestion"]

  - name: "gcr.io/cloud-builders/docker"
    id: push-dbt
    args: ["push", "--all-tags", "${_REGISTRY}/${_PROJECT}/${_REPO}/dbt"]
    waitFor: ["build-dbt"]

  # Jobs/workflow/scheduler are created once via gcloud (docs/gcp-setup.md steps 8-10).
  # Cloud Build only rolls the new image onto the existing jobs. No Terraform.
  - name: "gcr.io/google.com/cloudsdktool/cloud-sdk"
    id: deploy-appsflyer-job
    entrypoint: gcloud
    args:
      - run
      - jobs
      - update
      - extract-appsflyer
      - --image=${_REGISTRY}/${_PROJECT}/${_REPO}/ingestion:$COMMIT_SHA
      - --region=${_REGION}
      - --project=${_PROJECT}
    waitFor: ["push-ingestion"]

  - name: "gcr.io/google.com/cloudsdktool/cloud-sdk"
    id: deploy-moengage-job
    entrypoint: gcloud
    args:
      - run
      - jobs
      - update
      - extract-moengage
      - --image=${_REGISTRY}/${_PROJECT}/${_REPO}/ingestion:$COMMIT_SHA
      - --region=${_REGION}
      - --project=${_PROJECT}
    waitFor: ["push-ingestion"]

  - name: "gcr.io/google.com/cloudsdktool/cloud-sdk"
    id: deploy-play-console-job
    entrypoint: gcloud
    args:
      - run
      - jobs
      - update
      - extract-play-console
      - --image=${_REGISTRY}/${_PROJECT}/${_REPO}/ingestion:$COMMIT_SHA
      - --region=${_REGION}
      - --project=${_PROJECT}
    waitFor: ["push-ingestion"]

  - name: "gcr.io/google.com/cloudsdktool/cloud-sdk"
    id: deploy-dbt-job
    entrypoint: gcloud
    args:
      - run
      - jobs
      - update
      - dbt-transform
      - --image=${_REGISTRY}/${_PROJECT}/${_REPO}/dbt:$COMMIT_SHA
      - --region=${_REGION}
      - --project=${_PROJECT}
    waitFor: ["push-dbt"]

options:
  logging: CLOUD_LOGGING_ONLY
